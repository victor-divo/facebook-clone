const { Router } = require('express')
// const customerApiController = require('../controllers/api/customer')

const api = Router()

// api.use('/api/customer', customerApiController)

module.exports = api